# V2 Reality Snapshot — 2026-06-23

**Correction to my first pass:** I initially audited this as a "deployed app spanning Phases 0–7." That was wrong — it counted code-shaped files as shipped features. Verified truth below.

## Real — do not regress
- **AI mechanics engine** — prompt → provider → extract → validate → apply → gates. Files: `mechanics.js` (986L, 175 store-writes, 0 placeholders), `engine.js`, `gates.js`, `prompt.js`, `providers.js`, `memory.js`, `drift.js`. Tested, in active development.
- **Backstory** in character creation — real, active.

## Lost — must recover or redo
- **Onboarding (fuller flow)** — manual stat-rolling, import mapping, guided wizard. Existed in an **uncommitted** build that a deploy of `main` overwrote on the live site. Only quick-build + the backstory editor survive in git. Not recoverable from this repo (fresh clone, no reflog/stash).

## Mockup only — placeholder, rebuild for real
- The UI / layout / container on pebble-v2: `App.jsx` (4-button stub), `Chat.jsx` shell, play chrome. Throwaway. Real UI gets built on top of the working engine.

## Not built — stubs / unwired shells
- `Treasury.jsx` = **1 line**. `Compendium`, `Glossary`, `SessionReview`, `Contracts`, `ContentImport` = unwired. Reference mode, manage mode, multiplayer, push, child view, content-pipeline UI — none real yet.

## Lost-work recovery path (owner-only)
Firebase console → **Hosting → Release history**. The release from before yesterday's deploy still holds the lost onboarding build. Rolling it back / downloading it recovers the onboarding **and** its lost spec. Agent/CI can't reach Firebase — this step is yours.

## Root cause + guard
Docs drifted ~2 days → a deploy of committed `main` buried a more-complete *uncommitted* live build. Guard (already in CLAUDE.md): look at what's live before overwriting; commit before deploying; never trust a stale map.
